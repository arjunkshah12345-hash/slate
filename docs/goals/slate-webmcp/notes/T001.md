# T001 Scout

Empty greenfield. No package.json. Destination is Slate (Next.js App Router, client timeline, WebMCP).

Verification once scaffolded:
- `pnpm test`
- `pnpm build`
- `pnpm dev` + browser exercise of play / lock / export / tool simulate

Ranked slices:
1. Timeline engine + tests (must exist before UI)
2. WebMCP registry harness
3. Studio UI
4. Gauntlet inspect + critic
